#ifndef bf_v1_h
#define bf_v1_h

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* seek_forward(unsigned char* ptr, char* txt_ptr);
char* seek_back(unsigned char* ptr, char* txt_ptr);

#endif